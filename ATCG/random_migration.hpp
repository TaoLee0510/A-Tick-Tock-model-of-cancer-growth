#ifndef random_migration_hpp
#define random_migration_hpp

#include "cell_store.hpp"
#include "visual_range.hpp"

void random_migration(int i, double deltah, CellStore &cells, VisualRange &Visual_range, double &migration_judgement, long rng_time_step, long rng_event_base);

#endif
